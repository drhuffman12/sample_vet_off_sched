<%= form_for(@right) do |f| %>
  <% if @right.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@right.errors.count, "error") %> prohibited this right from being saved:</h2>

      <ul>
      <% @right.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%# = f.text_field :user_id %>
    <%= collection_select(:right, :user_id, User.all, :id, :name, {prompt: true}) %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br>
    <%# = f.text_field :role_id %>
    <%= collection_select(:right, :role_id, Role.all, :id, :name, {prompt: true}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
